// import web3 from '@/web3'
// import ipfs from '@/ipfs'

const state = {
   login : false
  };

  const getters = {
    login:(state) => { return state.login },
  };
  
  const actions = {
     

  };
  
  const mutations = { 

    
  };

  export default{
    state,
    getters,
    actions,
    mutations
}